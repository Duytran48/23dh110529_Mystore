@model _23dh110529_Mystore.Models.Category

@{
    ViewBag.Title = "Delete";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}

<h2 class="bg-success">Xóa danh mục</h2>

   
    
    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()
        <div>
            <h4 class="bg-danger">Mài có chắc mài muốn xóa tưa ? </h4>
           <div class="form-group">
                <dl class="row">
                    <dt class="col-sm-2">
                        @*@Html.DisplayNameFor(model => model.CategoryName)*@
                        Tên danh mục
                    </dt>
                    @* giá trị của trường CategoryName  *@
                    <dd class="col-sm-2">
                        @Html.DisplayFor(model => model.CategoryName)

                    </dd>

                </dl>
                <div class="form-group">
                    <div class="col-md-offset-2 col-md-10">
                        <input type="submit" value="Xóa đi" class=" btn btn-primary btn-sm" /> |
                        @Html.ActionLink("Quay lại danh sách", "Index", null, new { @class = " btn btn-primary btn-sm" })
                    </div>
                </div>
          </div>


        </div>
        
    }

